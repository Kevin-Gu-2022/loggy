# Instructions
1. Run the following to create a virtual environment:\
`python -m venv venv`

2. Then based on terminal:
    - Windows (PowerShell): ```venv\Scripts\Activate.ps1```
    - Mac/Linux: ```source venv/bin/activate```

3. Download the dependencies:\
`pip install -r requirements.txt`

Now you should be able to run all the code

